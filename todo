add a button that says "support me" thats links to donate.wikimedia.org
number of letters in each word of a term is shown below them, seperated by spaces
12 default colors are shown above the custom color section
change custom colors so a new recent is only added when you draw with it 
the logo in the top left should be a button to go back to the home page
round the points to multiples of 25
make it so you can click and drag the sliders on host settings
make the canvas bigger
the send button in the chat clips outside the chat box 
a text box in the round setup where you can add a new list

game settings as typed input instead of dropdown
    time limit (max 200)
    amount of rounds (max 20)
    maximum players (max 10)
    amount of word hints (max 10)

make the chat window bigger
make the x/x rounds indicator bigger
indicate which player is the artist 
be able to change name in the lobby screen 
for shorter terms, the hints should cap at half the word length
if your cursor goes outside the canvas when drawing, it should continue drawing when it re enters the canvas
there should be a rectangle and circle drawing tool 
each hint should only reveal a single letter 
fix hints overwrite the word for the artist 
fill tool should have some tolerance for the smoothing around the brush